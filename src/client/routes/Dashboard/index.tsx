import styled from 'styled-components';

// import { useAppTranslation } from '@hooks';

const DashboardView = styled.div`
  display: flex;
  flex: 1;
`;

export const Dashboard = () => {
  // const { t } = useAppTranslation('common');

  return <DashboardView>Dashboard</DashboardView>;
};
